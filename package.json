{
  "name": "ccnp-study-static",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx --yes vercel dev",
    "serve": "npx --yes serve public -l 3000 --no-port-switching"
  },
  "dependencies": {
    "@vercel/edge": "^1.2.2",
    "raw-body": "^3.0.0",
    "stripe": "^17.7.0"
  }
}
