---
description: Deep GA4 analytics for Security+ home (comptia-sec+-home.html) via MCP and /admin
globs: public/comptia-sec+-home.html,marketing-research/Sec+ Campaign/**,scripts/secplus*.md,public/js/secplus*.js
alwaysApply: false
---

# GA4 deep analytics — Security+ home

**Primary landing:** `/comptia-sec+-home.html` (`public/comptia-sec+-home.html`)  
**Property:** `538156526` · **Measurement:** `G-YTT6KBHX7V` · **GCP:** `youtube-404901`

When analytics MCP is connected, default all reports to this page unless the user asks otherwise.

## Active campaigns → this landing

| `utm_campaign` | Channel | Primary `utm_content` |
|----------------|---------|------------------------|
| `secplus_portal` | Google Ads | `core-exam-prep`, `mil-gov-8140`, `student-workforce` (legacy: `pbq-wedge`) |
| `secplus_wedge_pbq` | Reddit Ads | `reddit-pbq` |
| `secplus_portal` | Reddit (phase 2) | `reddit-timed-sim` |

**Checkout item ID:** `secplus_portal_30d` (`data-bcc-item-id` on purchase CTA)  
**Success page:** `/COMP_TIA_SEC+/secplus-portal-checkout-success.html` → `purchase` event

## MCP tools (prefer in this order)

1. `get_custom_dimensions_and_metrics` on `properties/538156526`
2. `run_report` — filter `pagePath` or `landingPage` = `/comptia-sec+-home.html`
3. `run_funnel_report` — `page_view` on home → `begin_checkout` (`secplus_portal_30d`) → `purchase`
4. `run_realtime_report` — live traffic on the home page
5. `list_google_ads_links` — Ads ↔ GA linkage for `secplus_portal`

## High-value questions for this page

| Question | Dimensions | Metrics | Filters |
|----------|------------|---------|---------|
| Ad → landing | `sessionCampaignName`, `sessionSource`, `sessionMedium` | `sessions`, `engagedSessions` | `landingPage` = `/comptia-sec+-home.html` |
| Checkout clicks | `sessionCampaignName` or `itemId` | `eventCount`, `activeUsers` | `eventName` = `begin_checkout`, `itemId` = `secplus_portal_30d` |
| Page engagement | `pagePath` | `screenPageViews`, `userEngagementDuration`, `activeUsers` | `pagePath` exact home |
| Popup funnel | `pagePath`, `eventName` | `eventCount` | `home_offer_popup_*`, `secplus_free_sim_start` |
| Sample → purchase | funnel steps | funnel metrics | 28d |

## Repo mirrors (when MCP unavailable)

- `/admin#section-secplus-ad-groups` — Security+ Google ad groups by `utm_content` (GA4)
- `/admin#section-recommendations` — conversion tactics from live metrics
- `/admin#section-daily-log` — owner daily spend/copy log (pairs with ad group changes)
- `server-lib/secplus-google-ad-groups.js` — ad group registry (Obsidian ↔ admin)
- `server-lib/site-traffic-report.js` — GA4 hub API
- `server-lib/campaign-marketing-registry.js` — all campaigns including `secplus_portal`
- `npm run marketing:weekly-report`

## Guardrails

- Member SEC+ library (`/COMP_TIA_SEC+/…`) is untagged except free-sample session.
- Do not paste service account JSON into chat or commits.
- Local verify (production data): open live home → click purchase → GA4 Realtime `begin_checkout`.
